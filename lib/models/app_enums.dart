enum UserRole { coach, admin, guest }

enum PlayerPosition { goalkeeper, defender, forward }

enum PlayerStatus { active, injured }

enum LineType { forward, defense, goalkeeper }

enum PlayerSortType { nameAsc, nameDesc, ageAsc, ageDesc }
